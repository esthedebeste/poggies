html(lang="en-US"){
    head{
        title[Test Page]
        style[
            #root{
                font-family: cursive;
            }
            .green{
                color: lime;
            }
            .red{
                color: red;
            }
            .bold{
                font-weight: bold;
            }
        ]
    }
    body#root{
        h1#hey-world.red.bold[Hello World!]
        p(
            class="bold green"
            data-close=")"
        )[:D]
        a(href=>"`https://${site}.`+tld")[>`${site}.${tld}`]
        p.red(hidden)[If this is visible, attributes aren't working properly.]
        br
    }
    <if(ifstatements.working())[Woah, if statements work!]>
    {br}
    <for(word of forTest.sentence)[>word]>
    {br}
    <for(double of doubleTest)<
       if(double.show)[>double.content]
    >>
}
multielemtest{
    div[Multiple Parent Elements work!]
}