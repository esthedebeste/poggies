html(lang="en-US"){
    head{
        title[Test Page]
        style[
            #root{
                font-family: cursive;
            }
            .green{
                color: lime;
            }
            .red{
                color: red;
            }
            .bold{
                font-weight: bold;
            }
        ]
    }
    body#root{
        h1#hey-world.red.bold[Hello World!]
        p(
            class="bold green"
            data-close=")"
        )[:D]
        a(href=>"`https://${site}.`+tld")[>`${site}.${tld}`]
        p.red(hidden)[If this is visible, attributes aren't working properly.]
        br
    }
    <if(ifstatements.working()){
        div[Woah, if statements work!]
    }
    for(word of sentence){
        span[>word]
    }>
}
multielemtest{
    div[Multiple Parent Elements work!]
}