html(lang="en-US"){
    head{
        title[Test Page]
        style[
            .green{
                color: lime;
            }
            .red{
                color: red;
            }
            .bold{
                font-weight: bold;
            }
        ]
    }
    body{
        h1(class="red bold")[Hello World!]
        p(
            class="bold green"
            data-close=")"
        )[:D]
        a(href=>"`https://${site}.`+tld")[>`${site}.${tld}`]
    }
}
